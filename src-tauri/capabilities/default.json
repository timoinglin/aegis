{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Aegis main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "opener:default",
    "opener:allow-open-url",
    "dialog:default",
    "dialog:allow-open"
  ]
}
