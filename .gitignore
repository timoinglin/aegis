# Dependencies
node_modules/

# Frontend build
dist/
*.tsbuildinfo
.vite/

# Package manager logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Rust / Tauri
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock.bak

# Note: src-tauri/icons/* ARE committed (placeholder set) so the repo builds
# out of the box. Regenerate with `npx tauri icon <art.png>` when the final logo lands.

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local working art (the non-final logo lives at repo root during dev)
/aegis_logo.png

# Updater signing keys + password — NEVER commit these (only the public key,
# which lives in tauri.conf.json, is safe to share). Store as CI secrets.
src-tauri/.keys/